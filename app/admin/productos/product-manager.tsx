"use client";
import { useMemo, useRef, useState } from "react";
import { formatGs, parseGs } from "../../../lib/format-gs";

const CATEGORIES = ["Electrónica", "Hogar", "Herramientas", "Vestimenta", "Belleza", "Accesorios", "Tecnología", "Otros"];
const normalizeCategory = (value: string | null | undefined) => { if (value === "Electro") return "Electrónica"; if (value === "Ropa") return "Vestimenta"; return value && CATEGORIES.includes(value) ? value : "Otros"; };
type Media = { id:string; media_type:"image"|"video"; url:string; original_name?:string|null; storage_path?:string|null; };
type Product = { id:string; name:string; price:number; cost:number|null; stock:number; category:string|null; description:string|null; image_url:string|null; video_url:string|null; active:boolean; created_at:string; updated_at:string; product_media?:Media[] };

export function ProductManager({ initialProducts }: { initialProducts: Product[] }) {
  const [products, setProducts] = useState(initialProducts); const [query,setQuery]=useState(""); const [filter,setFilter]=useState("todos"); const [editing,setEditing]=useState<Product|null>(null); const [busy,setBusy]=useState(false); const [message,setMessage]=useState("");
  const shown=useMemo(()=>products.filter(p=>{const matches=`${p.name} ${normalizeCategory(p.category)}`.toLowerCase().includes(query.toLowerCase());const state=filter==="todos"||(filter==="activos"&&p.active)||(filter==="inactivos"&&!p.active)||(filter==="bajo"&&p.stock<=5);return matches&&state;}),[products,query,filter]);
  async function save(product:Product){setBusy(true);setMessage("");try{const r=await fetch("/api/admin-products",{method:"PATCH",headers:{"Content-Type":"application/json",Accept:"application/json"},credentials:"include",cache:"no-store",body:JSON.stringify({...product,price:Number(product.price),cost:Number(product.cost||0),stock:Number(product.stock),category:normalizeCategory(product.category)})});const result=await r.json().catch(()=>({}));if(!r.ok)throw new Error(result?.error||"No se pudo guardar.");setProducts(list=>list.map(x=>x.id===result.product.id?result.product:x));setEditing(null);setMessage("✅ Producto actualizado correctamente.");}catch(e:any){setMessage(`❌ ${e?.message||"No se pudo guardar."}`);}finally{setBusy(false);}}
  async function toggle(p:Product){await save({...p,active:!p.active});}
  async function removeProduct(p:Product){if(!confirm(`¿Eliminar definitivamente “${p.name}”? Esta acción no se puede deshacer.`))return;setBusy(true);setMessage("");try{const r=await fetch("/api/admin-products/delete",{method:"DELETE",headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify({id:p.id})});const result=await r.json().catch(()=>({}));if(!r.ok)throw new Error(result?.error||"No se pudo eliminar.");setProducts(list=>list.filter(x=>x.id!==p.id));setMessage("✅ Producto eliminado del catálogo.");}catch(e:any){setMessage(`❌ ${e?.message||"No se pudo eliminar."}`);}finally{setBusy(false);}}
  return <><div className="panel product-toolbar" style={{borderRadius:20,boxShadow:"0 8px 28px rgba(33,23,26,.06)"}}><div className="twocol"><label>Buscar producto<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Nombre o categoría" /></label><label>Filtrar<select value={filter} onChange={e=>setFilter(e.target.value)}><option value="todos">Todos</option><option value="activos">Activos</option><option value="inactivos">Inactivos</option><option value="bajo">Stock bajo (≤ 5)</option></select></label></div><p className="muted">{shown.length} producto(s) mostrado(s) · {products.filter(p=>p.active).length} activo(s).</p></div>{message&&<div className="panel" role="status">{message}</div>}<div className="admin-products">{shown.map(p=><article className="panel admin-product" key={p.id} style={{borderRadius:20,boxShadow:"0 8px 28px rgba(33,23,26,.06)"}}><div className="admin-product-media" style={{width:140,height:140,maxWidth:"100%",overflow:"hidden",borderRadius:16,display:"flex",alignItems:"center",justifyContent:"center",background:"#f3ecec",flexShrink:0}}>{p.image_url?<img src={p.image_url} alt={p.name} loading="lazy" style={{width:"100%",height:"100%",objectFit:"contain",padding:10}}/>:<b>DF</b>}</div><div className="admin-product-main"><div className="admin-product-top"><div><small>{normalizeCategory(p.category)}</small><h2>{p.name}</h2></div><span className={p.active?"status active":"status"}>{p.active?"Activo":"Inactivo"}</span></div><div className="admin-product-data"><div><span>Precio</span><b>₲ {Number(p.price).toLocaleString("es-PY")}</b></div><div><span>Costo</span><b>₲ {Number(p.cost||0).toLocaleString("es-PY")}</b></div><div><span>Disponibilidad</span><b className={p.stock<=5?"low-stock":""}>{p.stock} unidad(es)</b></div></div><div className="actions"><button type="button" className="btn" disabled={busy} onClick={()=>{setMessage("");setEditing({...p,category:normalizeCategory(p.category)});}}>Editar producto</button><button type="button" className="btn secondary" disabled={busy} onClick={()=>toggle(p)}>{p.active?"Desactivar":"Activar"}</button><button type="button" className="btn secondary" disabled={busy} onClick={()=>removeProduct(p)}>Eliminar producto</button></div></div></article>)}</div>{!shown.length&&<div className="empty">No encontramos productos con ese filtro.</div>}{editing&&<EditProduct product={editing} busy={busy} onCancel={()=>setEditing(null)} onSave={save}/>}</>;
}

function EditProduct({product,busy,onCancel,onSave}:{product:Product;busy:boolean;onCancel:()=>void;onSave:(p:Product)=>void}){
 const [draft,setDraft]=useState({name:product.name,price:String(product.price??""),cost:String(product.cost??""),stock:String(product.stock??""),category:normalizeCategory(product.category),description:product.description||"",image_url:product.image_url||"",video_url:product.video_url||"",active:product.active}); const [media,setMedia]=useState<Media[]>(product.product_media||[]); const [uploading,setUploading]=useState(false); const fileRef=useRef<HTMLInputElement>(null); const set=(key:keyof typeof draft,value:string|boolean)=>setDraft(old=>({...old,[key]:value}));
 const submit=()=>onSave({...product,name:draft.name,price:parseGs(draft.price),cost:parseGs(draft.cost),stock:draft.stock===""?0:Math.max(0,Math.floor(Number(draft.stock))),category:normalizeCategory(draft.category),description:draft.description,image_url:draft.image_url,video_url:draft.video_url,active:draft.active,product_media:media});
 async function upload(files:FileList|null){if(!files?.length)return;setUploading(true);try{for(const file of Array.from(files)){const form=new FormData();form.append("product_id",product.id);form.append("file",file);const r=await fetch("/api/admin-product-media",{method:"POST",body:form,credentials:"include"});const result=await r.json().catch(()=>({}));if(!r.ok)throw new Error(result?.error||`No se pudo cargar ${file.name}.`);setMedia(old=>[...old,result.media]);}alert("Archivo(s) cargado(s) correctamente.");}catch(e:any){alert(e?.message||"No se pudo cargar el archivo.");}finally{setUploading(false);if(fileRef.current)fileRef.current.value="";}}
 async function removeMedia(item:Media){if(!confirm(`¿Eliminar ${item.media_type==="image"?"esta foto":"este video"}?`))return;const r=await fetch("/api/admin-product-media",{method:"DELETE",headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify({id:item.id})});const result=await r.json().catch(()=>({}));if(!r.ok){alert(result?.error||"No se pudo eliminar.");return;}setMedia(old=>old.filter(x=>x.id!==item.id));}
 return <div className="modal-backdrop" role="dialog" aria-modal="true" aria-label={`Editar ${draft.name}`} style={{position:"fixed",inset:0,zIndex:9999,background:"rgba(20,12,15,.62)",display:"flex",alignItems:"center",justifyContent:"center",padding:18,overflowY:"auto"}} onMouseDown={e=>{if(e.target===e.currentTarget)onCancel();}}><div className="panel edit-modal" style={{width:"min(760px,100%)",maxHeight:"calc(100vh - 36px)",overflowY:"auto",borderRadius:24,boxShadow:"0 24px 80px rgba(0,0,0,.28)",background:"#fff",padding:30}}><div className="title"><div><small>EDITAR PRODUCTO</small><h2>{draft.name||"Producto"}</h2></div><button className="linkbtn" type="button" onClick={onCancel}>Cerrar ✕</button></div><label>Nombre<input value={draft.name} onChange={e=>set("name",e.target.value)}/></label><div className="twocol"><label>Precio<input inputMode="numeric" value={formatGs(draft.price)} onChange={e=>set("price",String(parseGs(e.target.value)))}/></label><label>Costo<input inputMode="numeric" value={formatGs(draft.cost)} onChange={e=>set("cost",String(parseGs(e.target.value)))}/></label></div><div className="twocol"><label><b>Disponibilidad / Stock</b><input type="text" inputMode="numeric" value={draft.stock} onChange={e=>set("stock",e.target.value.replace(/[^0-9]/g,""))} placeholder="0"/></label><label>Categoría<select value={draft.category} onChange={e=>set("category",e.target.value)}>{CATEGORIES.map(c=><option key={c} value={c}>{c}</option>)}</select></label></div><label>Descripción<textarea value={draft.description} onChange={e=>set("description",e.target.value)}/></label><div style={{borderTop:"1px solid #eadfe0",marginTop:20,paddingTop:20}}><h3 style={{margin:"0 0 12px"}}>Fotos y videos del producto</h3><p className="muted">Podés cargar varias fotos y videos desde el teléfono o la computadora. También podés eliminarlos individualmente.</p><input ref={fileRef} type="file" accept="image/*,video/*" multiple onChange={e=>upload(e.target.files)} disabled={uploading||busy}/><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(110px,1fr))",gap:10,marginTop:15}}>{media.map(item=><div key={item.id} style={{border:"1px solid #eadfe0",borderRadius:12,padding:6,background:"#fff"}}>{item.media_type==="image"?<img src={item.url} alt={item.original_name||"Foto"} style={{width:"100%",height:90,objectFit:"contain"}}/>:<video src={item.url} controls style={{width:"100%",height:90,objectFit:"contain"}}/>}<button type="button" className="btn secondary" style={{width:"100%",marginTop:5,padding:"8px 5px",fontSize:12}} disabled={uploading||busy} onClick={()=>removeMedia(item)}>Eliminar</button></div>)}</div></div><label>URL de foto de portada<input value={draft.image_url} onChange={e=>set("image_url",e.target.value)} placeholder="https://..."/></label><label>URL de video principal<input value={draft.video_url} onChange={e=>set("video_url",e.target.value)} placeholder="https://..."/></label><label className="check"><input type="checkbox" checked={draft.active} onChange={e=>set("active",e.target.checked)}/> Producto activo</label><div className="actions"><button className="btn" type="button" disabled={busy||uploading} onClick={submit}>{busy?"Guardando...":"Guardar cambios"}</button><button className="btn secondary" type="button" disabled={busy||uploading} onClick={onCancel}>Cancelar</button></div></div></div>;
}
