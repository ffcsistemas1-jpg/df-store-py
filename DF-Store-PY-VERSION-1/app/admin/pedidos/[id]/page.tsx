"use client";
import Link from "next/link"; import {useEffect,useState} from "react"; import {useParams} from "next/navigation"; import {createClient} from "../../../../lib/supabase/browser";
const money=(n:number)=>`₲ ${Number(n||0).toLocaleString("es-PY")}`;
export default function Detalle(){const p=useParams();const id=String(p.id);const [o,setO]=useState<any>(null),[items,setItems]=useState<any[]>([]),[loading,setLoading]=useState(true),[msg,setMsg]=useState(""),[saving,setSaving]=useState(false);
 useEffect(()=>{(async()=>{const s=createClient();const [a,b]=await Promise.all([s.from("orders").select("*,customers(*),shipping_companies(name)").eq("id",id).single(),s.from("order_items").select("*").eq("order_id",id)]);if(a.error||b.error)setMsg(a.error?.message||b.error?.message||"");else{setO({...a.data,customer:a.data.customers,shipping_company:a.data.shipping_companies});setItems(b.data||[])}setLoading(false)})()},[id]);
 async function verify(value:boolean){setSaving(true);setMsg("");const s=createClient();const {data,error}=await s.from("orders").update({payment_verified:value,payment_verified_at:value?new Date().toISOString():null}).eq("id",id).select("*").single();if(error)setMsg(error.message);else setO((x:any)=>({...x,...data}));setSaving(false)}
 if(loading)return <section><div className="panel">Cargando...</div></section>;if(!o)return <section><div className="panel">{msg||"Pedido no encontrado"}</div></section>;
 return <section><div className="title"><div><small>PEDIDO #{id.slice(0,8).toUpperCase()}</small><h1>Detalle</h1></div><Link href="/admin/pedidos">← Pedidos</Link></div><div className="formgrid"><div className="panel"><h2>Cliente</h2><p><b>{o.customer?.full_name}</b></p><p>WhatsApp: {o.customer?.whatsapp}</p><p>Email: {o.customer?.email||"—"}</p><p>{o.customer?.department}, {o.customer?.city}</p><p>{o.customer?.neighborhood||""}</p><p>{o.customer?.address||"Retiro"}</p><h2>Entrega</h2><p>{o.delivery_type} · {o.shipping_company?.name||"Sin transportadora"}</p></div><div className="panel"><h2>Productos</h2>{items.map(i=><div className="checkout-total" key={i.id}><span>{i.product_name} × {i.quantity}</span><strong>{money(i.subtotal)}</strong></div>)}<div className="checkout-total"><span>Subtotal</span><strong>{money(o.subtotal)}</strong></div><div className="checkout-total"><span>Delivery</span><strong>{money(o.delivery_fee)}</strong></div><div className="checkout-total grand"><span>Total</span><strong>{money(o.total)}</strong></div><h2>Pago</h2><p>Método: <b>{o.payment_method}</b></p><p>Referencia: <b>{o.payment_reference||"No informada"}</b></p><p>Estado del pago: <b>{o.payment_verified?"VERIFICADO":"PENDIENTE DE VERIFICACIÓN"}</b></p>{o.payment_method!=="Pago al recibir"&&<button className="btn" disabled={saving} onClick={()=>verify(!o.payment_verified)}>{saving?"Guardando...":o.payment_verified?"Marcar como pendiente":"Verificar pago"}</button>}<p>Estado del pedido: <b>{o.status}</b></p></div></div></section>}
