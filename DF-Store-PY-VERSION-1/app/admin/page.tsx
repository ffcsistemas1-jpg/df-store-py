import Link from "next/link";
import {redirect} from "next/navigation";
import {AdminProductForm} from "./product-form";
import {createClient} from "../../lib/supabase/server";
export default async function Admin(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/admin/login");const {data:isAdmin}=await s.rpc("is_admin");if(!isAdmin)return <section><small>DF STORE PY</small><h1>Acceso denegado</h1><p>Tu cuenta ({user.email}) no tiene permisos de administrador.</p></section>;const [{count:pc},{data:orders},{data:products},{data:pending}]=await Promise.all([s.from("products").select("id",{count:"exact",head:true}).eq("active",true),s.from("orders").select("total").neq("status","cancelado"),s.from("products").select("stock").eq("active",true),s.from("orders").select("id").neq("payment_method","Pago al recibir").eq("payment_verified",false).neq("status","cancelado")]);const sales=(orders||[]).reduce((n:any,o:any)=>n+Number(o.total||0),0);const low=(products||[]).filter((p:any)=>Number(p.stock)<=5).length;return <section><div className="title"><div><small>DF STORE PY</small><h1>Administrador</h1></div><Link className="btn" href="/">Ver tienda</Link></div><div className="adminstats"><div><b>Productos activos</b><strong>{pc||0}</strong></div><div><b>Ventas</b><strong>₲ {sales.toLocaleString("es-PY")}</strong></div><div><b>Stock bajo</b><strong>{low}</strong></div><div><b>Pagos pendientes</b><strong>{pending?.length||0}</strong></div></div><div className="panel"><h2>➕ Nuevo producto</h2><p className="muted">Elegí la foto y opcionalmente un video. El sistema los sube automáticamente.</p><AdminProductForm/></div><div className="adminlinks">{[["Productos e inventario","/admin/productos"],["Promociones","/admin/promociones"],["Pedidos","/admin/pedidos"],["Embudo de ventas","/admin/embudo"],["Clientes","/admin/clientes"],["Métodos de pago","/admin/pagos"],["Delivery y zonas","/admin/delivery"],["Transportadoras","/admin/transportadoras"],["Reportes","/admin/reportes"],["Notificaciones","/admin/notificaciones"],["Configuración","/admin/configuracion"],["Seguridad","/admin"]].map(([x,h])=><Link key={x} href={h} className="panel"><h3>{x}</h3><span>Gestionar →</span></Link>)}</div></section>}
